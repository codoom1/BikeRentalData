% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bikerentaldata-package.R
\docType{package}
\name{bikerentaldata-package}
\alias{bikerentaldata-package}
\alias{bikerentaldata}
\title{bikerentaldata: Historical U.S. bike-share trip data tools}
\description{
\code{bikerentaldata} downloads and standardizes historical trip data from
Capital Bikeshare, Citi Bike, Divvy, and Bay Wheels without bundling the
full third-party datasets.
}
\section{Quick start}{

\enumerate{
\item Run \code{\link[=available_systems]{available_systems()}} to list supported systems.
\item Run \code{\link[=available_trip_data]{available_trip_data()}} to inspect archive coverage.
\item Run \code{\link[=download_trip_files]{download_trip_files()}} for a small date range.
\item Run \code{\link[=load_trip_data]{load_trip_data()}} to read and standardize the downloaded CSV files.
\item Run \code{\link[=trip_data_dictionary]{trip_data_dictionary()}} to inspect the output fields.
Use \code{\link[=build_multicity_data]{build_multicity_data()}} to perform these steps for several systems in
one call.
}
}

\section{Supported systems}{

Valid system identifiers are \code{"capital"} (Washington, D.C.),
\code{"citibike"} (New York City), \code{"divvy"} (Chicago), and \code{"baywheels"}
(San Francisco Bay Area).
}

\section{Capital Bikeshare paper workflow}{

\code{\link[=build_bike_rental_data]{build_bike_rental_data()}} downloads Capital Bikeshare trips and weather,
prepares daily records, validates them, and optionally writes a processed
CSV. \code{\link[=load_bike_rentals]{load_bike_rentals()}} and \code{\link[=validate_bike_rentals]{validate_bike_rentals()}} work with that
daily dataset.
}

\section{Calendar and weather enrichment}{

\code{\link[=add_calendar_variables]{add_calendar_variables()}} adds local calendar, time-of-day, season,
weekend, and observed U.S. federal holiday fields.
\code{\link[=add_weather_variables]{add_weather_variables()}} adds daily metro weather from default ASOS
stations. Weather represents the metro area, not a rider's exact route.
}

\section{Bicycle infrastructure exposure}{

The package can download official bicycle infrastructure layers and compute
station-area exposure variables. Use \code{\link[=download_bike_infrastructure]{download_bike_infrastructure()}} to get
the supported infrastructure layer for a system, and
\code{\link[=available_bike_infrastructure_sources]{available_bike_infrastructure_sources()}} to inspect the source, coverage
area, jurisdiction, facility-type field, and caveats.

Infrastructure exposure is measured around stations, not along the unknown
rider route. For example, \code{start_bikeinfra_500m_m} is the total length of
bicycle infrastructure within 500 meters of the trip's start station, and
\code{start_nearest_bikeinfra_m} is the straight-line distance from the start
station to the nearest bicycle facility. These variables should be
interpreted as nearby infrastructure availability, not proof that a rider
used a specific bike lane or trail.

Density variables ending in \verb{_m_per_km2} express infrastructure length per
square kilometer inside the station buffer and are recommended for
cross-city comparisons. Raw total length is useful, but dense large-city
networks can naturally produce higher totals.

Protected bike lanes are facilities separated from motor traffic by posts,
curbs, planters, parked cars, raised/off-street design, or similar
separation. Exact labels vary by city source layer.

For large systems such as Citi Bike, use the station-level cache workflow:
build a reusable station exposure table with
\code{\link[=build_station_infrastructure_exposure]{build_station_infrastructure_exposure()}}, join it to trips with
\code{\link[=add_station_infrastructure_exposure]{add_station_infrastructure_exposure()}}, and audit coverage with
\code{\link[=summarize_station_infrastructure_coverage]{summarize_station_infrastructure_coverage()}}. The convenience wrapper
\code{\link[=add_bike_infrastructure_exposure]{add_bike_infrastructure_exposure()}} performs the build-and-join workflow in
one call and accepts a \code{cache_file} argument.

Run \code{\link[=infrastructure_data_dictionary]{infrastructure_data_dictionary()}} for detailed definitions of the
infrastructure variables and \code{\link[=trip_data_dictionary]{trip_data_dictionary()}} for the full
standardized trip schema.
}

\section{Legacy support}{

Archive structures vary by system and year. The standardizer supports the
recent common Lyft schema and common legacy column names. Some legacy files
do not include bike type or coordinates, so unavailable fields are \code{NA}.
}

\section{Large downloads}{

Inspect \code{\link[=available_trip_data]{available_trip_data()}} before downloading. Some archives are large,
particularly annual Citi Bike archives. Use a recent one-month range and
the \code{n_max} argument to \code{\link[=load_trip_data]{load_trip_data()}} when testing the package.
}

\section{Data licensing}{

The package's MIT license applies only to original software. Capital
Bikeshare, Citi Bike, Divvy, Bay Wheels, and weather records remain subject
to their source terms.
}

\examples{
available_systems()
trip_data_dictionary()
infrastructure_data_dictionary()

\dontrun{
archives <- available_trip_data("divvy")
paths <- download_trip_files(
  system = "divvy",
  start_date = "2024-01-01",
  end_date = "2024-01-31",
  destination = "data/divvy"
)
trips <- load_trip_data(paths, system = "divvy", n_max = 1000)

bike_lanes <- download_bike_infrastructure("divvy")
station_exposure <- build_station_infrastructure_exposure(
  trips,
  bike_lanes,
  cache_file = "data/cache/divvy_station_exposure.csv"
)
trips <- add_station_infrastructure_exposure(trips, station_exposure)
}

}
\seealso{
\code{\link[=available_systems]{available_systems()}}, \code{\link[=available_trip_data]{available_trip_data()}}, \code{\link[=download_trip_files]{download_trip_files()}},
\code{\link[=load_trip_data]{load_trip_data()}}, \code{\link[=build_multicity_data]{build_multicity_data()}}, \code{\link[=trip_data_dictionary]{trip_data_dictionary()}},
\code{\link[=download_bike_infrastructure]{download_bike_infrastructure()}}, \code{\link[=build_station_infrastructure_exposure]{build_station_infrastructure_exposure()}},
\code{\link[=infrastructure_data_dictionary]{infrastructure_data_dictionary()}}
}
\author{
\strong{Maintainer}: Christopher Odoom \email{odoomchristopher22@gmail.com}

Authors:
\itemize{
  \item Christopher Odoom \email{odoomchristopher22@gmail.com}
}

}
