% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/infrastructure.R
\name{build_station_infrastructure_exposure}
\alias{build_station_infrastructure_exposure}
\title{Build station-level bicycle infrastructure exposure}
\usage{
build_station_infrastructure_exposure(
  trips,
  infrastructure,
  buffers_m = c(250, 500),
  facility_column = NULL,
  protected_values = NULL,
  trail_values = NULL,
  sides = c("start", "end"),
  projected_crs = NULL,
  cache_file = NULL,
  overwrite = FALSE
)
}
\arguments{
\item{trips}{Standardized trips containing station IDs/names and coordinates.}

\item{infrastructure}{An \code{sf} line layer, usually from
\code{download_bike_infrastructure()}.}

\item{buffers_m}{Numeric buffer distances, in meters.}

\item{facility_column}{Optional infrastructure facility-type column.}

\item{protected_values}{Values in \code{facility_column} counted as protected
infrastructure.}

\item{trail_values}{Values in \code{facility_column} counted as trails or
shared-use paths.}

\item{sides}{Which station sides to compute: \code{"start"}, \code{"end"}, or both.}

\item{projected_crs}{Optional projected CRS for distance/length operations.}

\item{cache_file}{Optional CSV file path where the station exposure table is
written and reused.}

\item{overwrite}{Whether to recompute an existing \code{cache_file}.}
}
\value{
A station-level tibble with one row per station side/key.
}
\description{
Computes reusable station-level bike-infrastructure exposure tables from
standardized trip records and an \code{sf} line layer.
}
\details{
This is the recommended workflow for large or repeated monthly/yearly
studies. Exposure is station-level, so computing it once and joining it back
to many trip files is much faster and easier to audit.
}
\examples{
\dontrun{
lanes <- download_bike_infrastructure("divvy")
station_exposure <- build_station_infrastructure_exposure(
  trips,
  lanes,
  cache_file = "data/cache/divvy_station_exposure.csv"
)
trips <- add_station_infrastructure_exposure(trips, station_exposure)
}
}
